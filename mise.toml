[tools]
hadolint = "latest"
shellcheck = "latest"
jq = "latest"
yq = "latest"

[task_config]
includes = ["wt-worker/mise-tasks.toml"]

[tasks.lint]
description = "Lint all plugins"
depends = ["wt-worker:lint"]
